{
  "ignoreCommand": "git diff --quiet \"${VERCEL_GIT_PREVIOUS_SHA:-HEAD^}\" HEAD -- apps/web packages/shared package.json package-lock.json",
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
