{
  "ignoreCommand": "git diff --quiet \"${VERCEL_GIT_PREVIOUS_SHA:-HEAD^}\" HEAD -- apps/health-web package.json package-lock.json",
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
