# home / v1 / source

Reserved for the owner-approved source design file(s) for the Asclepios
Health Home page, version 1 (e.g. the original layered export the
`approved-home-reference.png` and the `web/`/`mobile/` derivatives are
produced from).

**Status: empty.** No owner-approved source asset has been supplied for
this page/version yet. Do not add a file here without first recording it
in `../manifest.json`'s `assets` array with `owner_approved: true` and a
non-empty `approval_record` pointing at the durable owner-approval
evidence. See `docs/automation/VISUAL_ASSET_MANIFEST_V1_1.md`.
